name: Build IPAVIET App

on:
  workflow_dispatch:

jobs:
  build:
    name: Build iOS App
    runs-on: macos-13
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode_14.3.1.app/Contents/Developer

      - name: Build and Package
        run: |
          # 1. Ép Build không cần chứng chỉ Apple
          xcodebuild clean build \
            -project ios-pwa-wrap.xcodeproj \
            -scheme ios-pwa-wrap \
            -configuration Release \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            SYMROOT=$PWD/build

          # 2. Đóng gói thành file IPA
          mkdir -p Payload
          mv build/Release-iphoneos/*.app Payload/
          zip -r IPAVIET_Store.ipa Payload

      - name: Upload IPA Artifact
        uses: actions/upload-artifact@v3
        with:
          name: IPAVIET_App_V1
          path: IPAVIET_Store.ipa
